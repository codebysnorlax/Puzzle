{
  "name": "image-puzzle-pwa",
  "private": true,
  "version": "1.58.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=rough-frog-617d",
    "dev:pages": "wrangler pages dev dist --kv=STATS_KV --compatibility-date=2024-01-01"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  },
  "dependencies": {
    "pixi.js": "^8.6.6"
  }
}
